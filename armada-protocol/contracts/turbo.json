{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["**/.env.*local"],
  "globalEnv": ["NODE_ENV"],
  "pipeline": {
    "dev": {
      "cache": false,
      "persistent": true
    },
    "prebuild": {
      "dependsOn": ["^prebuild", "^prebuild:nocache"],
      "outputs": ["src/types/graphql/generated.ts", "src/generated"]
    },
    "prebuild:nocache": {
      "dependsOn": ["^prebuild:nocache"],
      "cache": false
    },
    "build": {
      "dependsOn": ["prebuild", "^build"],
      "outputs": ["dist/**", "artifacts/**", "out/**", "cache/**"]
    },
    "test": {
      "dependsOn": ["prebuild", "build", "^test"],
      "outputs": ["out/**", "cache/**"]
    },
    "test:integration": {
      "dependsOn": ["prebuild", "build", "^test:integration"]
    },
    "lint": {
      "dependsOn": ["prebuild", "^lint"]
    },
    "lint:fix": {
      "dependsOn": ["prebuild", "build", "^lint:fix"]
    },
    "format": {
      "dependsOn": ["^format"]
    },
    "check-circular": {
      "dependsOn": ["prebuild", "build", "^check-circular"]
    },
    "cicheck": {
      "dependsOn": [
        "prebuild",
        "build",
        "test",
        "test:integration",
        "lint",
        "check-circular",
        "format",
        "coverage:total"
      ]
    },
    "deploy:staging": {
      "dependsOn": ["cicheck", "^deploy:staging"]
    },
    "coverage": {
      "dependsOn": ["test", "test:integration", "^coverage"]
    },
    "coverage:lcov": {
      "dependsOn": ["build"],
      "outputs": ["lcov.info", "out/**", "cache/**"]
    }
  }
}
