// AUTOGENERATED FILE - DO NOT EDIT
import { ContractWrapper } from '../../implementation/contracts/ContractWrapper'
import { IBlockchainClient } from '@thesolidchain/blockchain-client-common'
import { IAddress, TransactionInfo, AddressValue } from '@thesolidchain/sdk-common'
import { ERC20V4_9_6Abi } from './abis/ERC20V4_9_6Abi'
import { IERC20V4_9_6 } from '@thesolidchain/contracts-provider-common'

export class ERC20V4_9_6<TClient extends IBlockchainClient, TAddress extends IAddress> extends ContractWrapper<typeof ERC20V4_9_6Abi, TClient, TAddress> implements IERC20V4_9_6 {
  public constructor(params: { blockchainClient: TClient; chainInfo: import('@thesolidchain/sdk-common').IChainInfo; address: TAddress }) {
    super(params)
  }

  getAbi() { return ERC20V4_9_6Abi }

  /**
     * @dev Returns the remaining number of tokens that `spender` will be
     * allowed to spend on behalf of `owner` through {transferFrom}. This is
     * zero by default.
     *
     * This value changes when {approve} or {transferFrom} are called.
     */
  public async allowance(owner: AddressValue, spender: AddressValue): Promise<bigint> {
    return this._contract.read.allowance([owner, spender]) as Promise<bigint>
  }

  /**
     * @dev Sets `amount` as the allowance of `spender` over the caller's tokens.
     *
     * Returns a boolean value indicating whether the operation succeeded.
     *
     * IMPORTANT: Beware that changing an allowance with this method brings the risk
     * that someone may use both the old and the new allowance by unfortunate
     * transaction ordering. One possible solution to mitigate this race
     * condition is to first reduce the spender's allowance to 0 and set the
     * desired value afterwards:
     * https://github.com/ethereum/EIPs/issues/20#issuecomment-263524729
     *
     * Emits an {Approval} event.
     */
  public async approve(spender: AddressValue, amount: bigint): Promise<TransactionInfo> {
    return this._createTransaction({
      functionName: 'approve',
      args: [spender, amount],
      description: 'approve execution'
    })
  }

  /**
     * @dev Returns the amount of tokens of token type `id` owned by `account`.
     *
     * Requirements:
     *
     * - `account` cannot be the zero address.
     */
  public async balanceOf(account: AddressValue): Promise<bigint> {
    return this._contract.read.balanceOf([account]) as Promise<bigint>
  }

  /**
     * @dev Returns the number of decimals used to get its user representation.
     * For example, if `decimals` equals `2`, a balance of `505` tokens should
     * be displayed to a user as `5.05` (`505 / 10 ** 2`).
     *
     * Tokens usually opt for a value of 18, imitating the relationship between
     * Ether and Wei. This is the default value returned by this function, unless
     * it's overridden.
     *
     * NOTE: This information is only used for _display_ purposes: it in
     * no way affects any of the arithmetic of the contract, including
     * {IERC20-balanceOf} and {IERC20-transfer}.
     */
  public async decimals(): Promise<number> {
    return this._contract.read.decimals() as Promise<number>
  }

  /**
     * @dev Atomically decreases the allowance granted to `spender` by the caller.
     *
     * This is an alternative to {approve} that can be used as a mitigation for
     * problems described in {IERC20-approve}.
     *
     * Emits an {Approval} event indicating the updated allowance.
     *
     * Requirements:
     *
     * - `spender` cannot be the zero address.
     * - `spender` must have allowance for the caller of at least
     * `subtractedValue`.
     */
  public async decreaseAllowance(spender: AddressValue, subtractedValue: bigint): Promise<TransactionInfo> {
    return this._createTransaction({
      functionName: 'decreaseAllowance',
      args: [spender, subtractedValue],
      description: 'decreaseAllowance execution'
    })
  }

  /**
     * @dev Atomically increases the allowance granted to `spender` by the caller.
     *
     * This is an alternative to {approve} that can be used as a mitigation for
     * problems described in {IERC20-approve}.
     *
     * Emits an {Approval} event indicating the updated allowance.
     *
     * Requirements:
     *
     * - `spender` cannot be the zero address.
     */
  public async increaseAllowance(spender: AddressValue, addedValue: bigint): Promise<TransactionInfo> {
    return this._createTransaction({
      functionName: 'increaseAllowance',
      args: [spender, addedValue],
      description: 'increaseAllowance execution'
    })
  }

  /**
     * @notice module:core
     * @dev Name of the governor instance (used in building the ERC712 domain separator).
     */
  public async name(): Promise<string> {
    return this._contract.read.name() as Promise<string>
  }

  /**
     * @dev Returns the symbol of the token, usually a shorter version of the
     * name.
     */
  public async symbol(): Promise<string> {
    return this._contract.read.symbol() as Promise<string>
  }

  /**
     * @dev Total amount of tokens in with a given id.
     */
  public async totalSupply(): Promise<bigint> {
    return this._contract.read.totalSupply() as Promise<bigint>
  }

  /**
     * @dev Moves `amount` tokens from the caller's account to `to`.
     *
     * Returns a boolean value indicating whether the operation succeeded.
     *
     * Emits a {Transfer} event.
     */
  public async transfer(to: AddressValue, amount: bigint): Promise<TransactionInfo> {
    return this._createTransaction({
      functionName: 'transfer',
      args: [to, amount],
      description: 'transfer execution'
    })
  }

  /**
     * @dev Moves `amount` tokens from `from` to `to` using the
     * allowance mechanism. `amount` is then deducted from the caller's
     * allowance.
     *
     * Returns a boolean value indicating whether the operation succeeded.
     *
     * Emits a {Transfer} event.
     */
  public async transferFrom(from: AddressValue, to: AddressValue, amount: bigint): Promise<TransactionInfo> {
    return this._createTransaction({
      functionName: 'transferFrom',
      args: [from, to, amount],
      description: 'transferFrom execution'
    })
  }

}
