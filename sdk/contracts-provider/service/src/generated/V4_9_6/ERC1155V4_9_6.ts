// AUTOGENERATED FILE - DO NOT EDIT
import { ContractWrapper } from '../../implementation/contracts/ContractWrapper'
import { IBlockchainClient } from '@summerfi/blockchain-client-common'
import { IAddress, TransactionInfo, AddressValue } from '@summerfi/sdk-common'
import { ERC1155V4_9_6Abi } from './abis/ERC1155V4_9_6Abi'
import { IERC1155V4_9_6 } from './interfaces/IERC1155V4_9_6'

export class ERC1155V4_9_6<TClient extends IBlockchainClient, TAddress extends IAddress> extends ContractWrapper<typeof ERC1155V4_9_6Abi, TClient, TAddress> implements IERC1155V4_9_6 {
  public constructor(params: { blockchainClient: TClient; chainInfo: import('@summerfi/sdk-common').IChainInfo; address: TAddress }) {
    super(params)
  }

  getAbi() { return ERC1155V4_9_6Abi }

  /**
     * @dev Returns the amount of tokens of token type `id` owned by `account`.
     *
     * Requirements:
     *
     * - `account` cannot be the zero address.
     */
  public async balanceOf(account: AddressValue, id: bigint): Promise<bigint> {
    return this._contract.read.balanceOf([account, id]) as Promise<bigint>
  }

  /**
     * @dev xref:ROOT:erc1155.adoc#batch-operations[Batched] version of {balanceOf}.
     *
     * Requirements:
     *
     * - `accounts` and `ids` must have the same length.
     */
  public async balanceOfBatch(accounts: readonly AddressValue[], ids: readonly bigint[]): Promise<readonly bigint[]> {
    return this._contract.read.balanceOfBatch([accounts, ids]) as Promise<readonly bigint[]>
  }

  /**
     * @dev Returns true if `operator` is approved to transfer ``account``'s tokens.
     *
     * See {setApprovalForAll}.
     */
  public async isApprovedForAll(account: AddressValue, operator: AddressValue): Promise<boolean> {
    return this._contract.read.isApprovedForAll([account, operator]) as Promise<boolean>
  }

  /**
     * @dev xref:ROOT:erc1155.adoc#batch-operations[Batched] version of {safeTransferFrom}.
     *
     * Emits a {TransferBatch} event.
     *
     * Requirements:
     *
     * - `ids` and `amounts` must have the same length.
     * - If `to` refers to a smart contract, it must implement {IERC1155Receiver-onERC1155BatchReceived} and return the
     * acceptance magic value.
     */
  public async safeBatchTransferFrom(from: AddressValue, to: AddressValue, ids: readonly bigint[], amounts: readonly bigint[], data: `0x${string}`): Promise<TransactionInfo> {
    return this._createTransaction({
      functionName: 'safeBatchTransferFrom',
      args: [from, to, ids, amounts, data],
      description: 'safeBatchTransferFrom execution'
    })
  }

  /**
     * @dev Transfers `amount` tokens of token type `id` from `from` to `to`.
     *
     * Emits a {TransferSingle} event.
     *
     * Requirements:
     *
     * - `to` cannot be the zero address.
     * - If the caller is not `from`, it must have been approved to spend ``from``'s tokens via {setApprovalForAll}.
     * - `from` must have a balance of tokens of type `id` of at least `amount`.
     * - If `to` refers to a smart contract, it must implement {IERC1155Receiver-onERC1155Received} and return the
     * acceptance magic value.
     */
  public async safeTransferFrom(from: AddressValue, to: AddressValue, id: bigint, amount: bigint, data: `0x${string}`): Promise<TransactionInfo> {
    return this._createTransaction({
      functionName: 'safeTransferFrom',
      args: [from, to, id, amount, data],
      description: 'safeTransferFrom execution'
    })
  }

  /**
     * @dev Grants or revokes permission to `operator` to transfer the caller's tokens, according to `approved`,
     *
     * Emits an {ApprovalForAll} event.
     *
     * Requirements:
     *
     * - `operator` cannot be the caller.
     */
  public async setApprovalForAll(operator: AddressValue, approved: boolean): Promise<TransactionInfo> {
    return this._createTransaction({
      functionName: 'setApprovalForAll',
      args: [operator, approved],
      description: 'setApprovalForAll execution'
    })
  }

  /**
     * @dev Returns true if `account` supports the interface defined by
     * `interfaceId`. Support for {IERC165} itself is queried automatically.
     *
     * See {IERC165-supportsInterface}.
     */
  public async supportsInterface(interfaceId: `0x${string}`): Promise<boolean> {
    return this._contract.read.supportsInterface([interfaceId]) as Promise<boolean>
  }

  /**
     * @dev Returns the URI for token type `id`.
     *
     * If the `\{id\}` substring is present in the URI, it must be replaced by
     * clients with the actual token type ID.
     */
  public async uri(arg0: bigint): Promise<string> {
    return this._contract.read.uri([arg0]) as Promise<string>
  }

}
