// AUTOGENERATED FILE - DO NOT EDIT
import { ContractWrapper } from '../../implementation/contracts/ContractWrapper'
import { IBlockchainClient } from '@summerfi/blockchain-client-common'
import { IAddress, TransactionInfo, AddressValue } from '@summerfi/sdk-common'
import { ERC721V4_9_6Abi } from './abis/ERC721V4_9_6Abi'
import { IERC721V4_9_6 } from '@summerfi/contracts-provider-common'

export class ERC721V4_9_6<TClient extends IBlockchainClient, TAddress extends IAddress> extends ContractWrapper<typeof ERC721V4_9_6Abi, TClient, TAddress> implements IERC721V4_9_6 {
  public constructor(params: { blockchainClient: TClient; chainInfo: import('@summerfi/sdk-common').IChainInfo; address: TAddress }) {
    super(params)
  }

  getAbi() { return ERC721V4_9_6Abi }

  /**
     * @dev Sets `amount` as the allowance of `spender` over the caller's tokens.
     *
     * Returns a boolean value indicating whether the operation succeeded.
     *
     * IMPORTANT: Beware that changing an allowance with this method brings the risk
     * that someone may use both the old and the new allowance by unfortunate
     * transaction ordering. One possible solution to mitigate this race
     * condition is to first reduce the spender's allowance to 0 and set the
     * desired value afterwards:
     * https://github.com/ethereum/EIPs/issues/20#issuecomment-263524729
     *
     * Emits an {Approval} event.
     */
  public async approve(to: AddressValue, tokenId: bigint): Promise<TransactionInfo> {
    return this._createTransaction({
      functionName: 'approve',
      args: [to, tokenId],
      description: 'approve execution'
    })
  }

  /**
     * @dev Returns the amount of tokens of token type `id` owned by `account`.
     *
     * Requirements:
     *
     * - `account` cannot be the zero address.
     */
  public async balanceOf(owner: AddressValue): Promise<bigint> {
    return this._contract.read.balanceOf([owner]) as Promise<bigint>
  }

  /**
     * @dev Returns the account approved for `tokenId` token.
     *
     * Requirements:
     *
     * - `tokenId` must exist.
     */
  public async getApproved(tokenId: bigint): Promise<AddressValue> {
    return this._contract.read.getApproved([tokenId]) as Promise<AddressValue>
  }

  /**
     * @dev Returns true if `operator` is approved to transfer ``account``'s tokens.
     *
     * See {setApprovalForAll}.
     */
  public async isApprovedForAll(owner: AddressValue, operator: AddressValue): Promise<boolean> {
    return this._contract.read.isApprovedForAll([owner, operator]) as Promise<boolean>
  }

  /**
     * @notice module:core
     * @dev Name of the governor instance (used in building the ERC712 domain separator).
     */
  public async name(): Promise<string> {
    return this._contract.read.name() as Promise<string>
  }

  /**
     * @dev Returns the owner of the `tokenId` token.
     *
     * Requirements:
     *
     * - `tokenId` must exist.
     */
  public async ownerOf(tokenId: bigint): Promise<AddressValue> {
    return this._contract.read.ownerOf([tokenId]) as Promise<AddressValue>
  }

  /**
     * @dev Transfers `amount` tokens of token type `id` from `from` to `to`.
     *
     * Emits a {TransferSingle} event.
     *
     * Requirements:
     *
     * - `to` cannot be the zero address.
     * - If the caller is not `from`, it must have been approved to spend ``from``'s tokens via {setApprovalForAll}.
     * - `from` must have a balance of tokens of type `id` of at least `amount`.
     * - If `to` refers to a smart contract, it must implement {IERC1155Receiver-onERC1155Received} and return the
     * acceptance magic value.
     */
  public async safeTransferFrom(from: AddressValue, to: AddressValue, tokenId: bigint): Promise<TransactionInfo> {
    return this._createTransaction({
      functionName: 'safeTransferFrom',
      args: [from, to, tokenId],
      description: 'safeTransferFrom execution'
    })
  }

  /**
     * @dev Grants or revokes permission to `operator` to transfer the caller's tokens, according to `approved`,
     *
     * Emits an {ApprovalForAll} event.
     *
     * Requirements:
     *
     * - `operator` cannot be the caller.
     */
  public async setApprovalForAll(operator: AddressValue, approved: boolean): Promise<TransactionInfo> {
    return this._createTransaction({
      functionName: 'setApprovalForAll',
      args: [operator, approved],
      description: 'setApprovalForAll execution'
    })
  }

  /**
     * @dev Returns true if `account` supports the interface defined by
     * `interfaceId`. Support for {IERC165} itself is queried automatically.
     *
     * See {IERC165-supportsInterface}.
     */
  public async supportsInterface(interfaceId: `0x${string}`): Promise<boolean> {
    return this._contract.read.supportsInterface([interfaceId]) as Promise<boolean>
  }

  /**
     * @dev Returns the symbol of the token, usually a shorter version of the
     * name.
     */
  public async symbol(): Promise<string> {
    return this._contract.read.symbol() as Promise<string>
  }

  /**
     * @dev Returns the Uniform Resource Identifier (URI) for `tokenId` token.
     */
  public async tokenURI(tokenId: bigint): Promise<string> {
    return this._contract.read.tokenURI([tokenId]) as Promise<string>
  }

  /**
     * @dev Moves `amount` tokens from `from` to `to` using the
     * allowance mechanism. `amount` is then deducted from the caller's
     * allowance.
     *
     * Returns a boolean value indicating whether the operation succeeded.
     *
     * Emits a {Transfer} event.
     */
  public async transferFrom(from: AddressValue, to: AddressValue, tokenId: bigint): Promise<TransactionInfo> {
    return this._createTransaction({
      functionName: 'transferFrom',
      args: [from, to, tokenId],
      description: 'transferFrom execution'
    })
  }

}
